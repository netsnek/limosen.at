# Runtime for the post-deploy gate of limosen.at.
#
# Install into tests/.venv, which is gitignored:
#   python3 -m venv tests/.venv
#   tests/.venv/bin/pip install -r tests/requirements.txt
#   tests/.venv/bin/playwright install chromium
#
# Pinned to what the suite was last executed with here (Python 3.14, aarch64)
# and to versions that also install on the runner's Python 3.12, which is what
# .github/workflows/post-deploy.yaml uses. nbconvert stays on 7.x: the gate
# depends on `--execute` ending non-zero when a cell raises.

jupyter==1.1.1
nbconvert==7.17.1
nbclient==0.11.0
nbformat==5.11.0
ipykernel==7.3.0
jupyter_client==8.9.1
jupyter_core==5.9.1

# The browser half, notebook 02. The browser itself is a separate download:
# `playwright install chromium`, or `playwright install --with-deps chromium`
# on the runner.
playwright==1.62.0
